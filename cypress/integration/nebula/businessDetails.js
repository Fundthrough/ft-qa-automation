describe('Business Details', () => {
    beforeEach(() => {
        cy.fixture('profile').then(function (user) {
            this.user = user;
        })
    })

    it('Validate business details flow', () => {
        
    })
})